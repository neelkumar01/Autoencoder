### Autoencoder

The model learns to compress each 28×28 grayscale image into a compact 16 dimensional latent representation and then reconstruct the original image from this compressed encoding

Through this process the network discovers the most important features of each digit while learning an efficient representation of the data
